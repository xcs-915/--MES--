/**
 * TNS MES Frontend Configuration
 * Edit this file to configure the frontend for your deployment.
 */
window.TNS_MES_CONFIG = {
  // API base URL for backend server
  // Use empty string for same-origin (when frontend is served by the backend)
  // Example: 'https://api.example.com/tns-mes'
  apiBaseUrl: '',
  
  // Context path (used for static assets when served under a sub-path)
  // Use empty string for root deployment
  contextPath: ''
};
