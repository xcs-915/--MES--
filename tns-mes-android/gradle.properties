# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override* any settings specified in this file.

# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8

# When configured, Gradle runs in incubating parallel mode.
# This option should only be used with decoupled projects. More details, visit
# https://developer.android.com/r/tools/gradle-multi-project-studio
org.gradle.parallel=true

# AndroidX package structure makes it easier to update your app.
android.useAndroidX=true

# Kotlin code style for this project.
kotlin.code.style=official

# Enables namespacing of each library's R class so that its R class includes only the
# resources declared in the library itself and resources from dependencies.
android.nonTransitiveRClass=true
