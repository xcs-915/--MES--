spring:
  datasource:
    url: jdbc:h2:mem:tns_mes_test;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false
  data:
    redis:
      host: 127.0.0.1
      port: 6399
mes:
  security:
    jwt-secret: test-secret-for-jwt-signing-that-is-long-enough
