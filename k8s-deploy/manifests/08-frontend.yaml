# ==============================================================================
# 08-frontend.yaml (已废弃 - 单体模式不再需要独立前端 Pod)
# ==============================================================================
# [架构说明] 当前为单体模式: 后端 tns-mes-server (NodePort 31180) 统一提供前端页面与 API, 不再使用 Gateway/微服务/独立前端 Pod
# --------------------------------------------------------------------------------
# 废弃说明:
#   原内容为独立前端 Pod (tns-frontend) 的 Deployment / Service / PDB,
#   通过 Nginx 服务静态文件并反向代理到 SpringCloud Gateway。
#   当前单体后端 tns-mes-server 已内置前端页面与 API, 直接通过 NodePort 31180
#   对外提供, 独立前端 Pod 属于冗余, 故移除 Deployment / Service / PDB 全部定义。
#   单体后端部署见 12-backend-deploy.yaml。
# 保留此文件仅作架构变更记录, 便于历史追溯; 不包含任何 K8s 资源。
# 清理集群中已存在的前端资源请执行: bash scripts/00-cleanup.sh
# ==============================================================================
# (本文件不包含任何 K8s 资源定义, 可安全保留于 manifests 目录)
