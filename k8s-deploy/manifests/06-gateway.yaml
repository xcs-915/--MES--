# ==============================================================================
# 06-gateway.yaml (已废弃 - 单体模式不再需要网关)
# ==============================================================================
# [架构说明] 当前为单体模式: 后端 tns-mes-server (NodePort 31180) 统一提供前端页面与 API, 不再使用 Gateway/微服务/独立前端 Pod
# --------------------------------------------------------------------------------
# 废弃说明:
#   原内容为 SpringCloud Gateway (sc-gateway) 的 Deployment / Service / PDB,
#   用于微服务架构下的统一路由与限流入口 (NodePort 31080)。
#   当前采用单体后端架构, 由 tns-mes-server 直接对外提供页面与 API,
#   不再需要独立的 SpringCloud Gateway, 故移除全部资源定义。
#   单体后端部署见 12-backend-deploy.yaml。
# 保留此文件仅作架构变更记录, 便于历史追溯; 不包含任何 K8s 资源。
# 清理集群中已存在的 Gateway 资源请执行: bash scripts/00-cleanup.sh
# ==============================================================================
# (本文件不包含任何 K8s 资源定义, 可安全保留于 manifests 目录)
