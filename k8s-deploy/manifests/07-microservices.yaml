# ==============================================================================
# 07-microservices.yaml (已废弃 - 单体模式不再需要微服务)
# ==============================================================================
# [架构说明] 当前为单体模式: 后端 tns-mes-server (NodePort 31180) 统一提供前端页面与 API, 不再使用 Gateway/微服务/独立前端 Pod
# --------------------------------------------------------------------------------
# 废弃说明:
#   原内容为 6 个微服务的 Deployment / Service / PDB:
#     - tns-auth-service        (8081)
#     - tns-product-service      (8082)
#     - tns-workorder-service    (8083)
#     - tns-quality-service      (8084)
#     - tns-integration-service  (8085)
#     - tns-basic-data-service   (8086)
#   当前采用单体后端架构, 上述能力已合并进 tns-mes-server 单体服务,
#   不再需要独立微服务, 故移除全部资源定义。
#   单体后端部署见 12-backend-deploy.yaml。
# 保留此文件仅作架构变更记录, 便于历史追溯; 不包含任何 K8s 资源。
# 清理集群中已存在的微服务 (缩减副本到 0) 请执行: bash scripts/00-cleanup.sh
# ==============================================================================
# (本文件不包含任何 K8s 资源定义, 可安全保留于 manifests 目录)
